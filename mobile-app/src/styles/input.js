import { StyleSheet } from "react-native";


const input = StyleSheet.create({
  style: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
    width: '100%',
  },
});

export default input;
